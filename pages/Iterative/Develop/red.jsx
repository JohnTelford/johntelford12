
export const Red = ({message}) => {
    return <div style={{
        padding: '15px',
        backgroundColor: 'red',
        color: 'white'
    }} >
        {message}
    </div>
}
    export default Red;
